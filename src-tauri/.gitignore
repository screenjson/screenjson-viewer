# Generated by Tauri
target/
gen/schemas/

# iOS / Android scaffolding is generated by `tauri ios init` / `tauri android init`.
# Keep the generated project in git once initialized so CI can build without re-running init.
# Uncomment if you prefer not to track them:
# gen/apple/
# gen/android/
